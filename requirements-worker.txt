# requirements-worker.txt — kimiSec 环境配置依赖

# Kimi Agent SDK（核心依赖）
# 如果需要使用内部源可以取消下面这行的注释并修改
# --extra-index-url https://your-internal-pypi.example.com/simple/
kimi-agent-sdk

# TUI 界面及其美化依赖
textual>=0.40.0
rich>=13.0.0

# YAML 解析（如需读取外部配置文件等）
pyyaml>=6.0.0
