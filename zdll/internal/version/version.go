// Package version contains the single source of truth for zdll version strings.
package version

const (
	// CLI is the command-line tool version.
	CLI = "0.1.0"

	// Engine is the analysis engine identifier persisted in blackboard snapshots.
	Engine = "HIVE-MIND INTEL ENGINE V8.0"
)
